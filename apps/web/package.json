{
  "name": "@regicide/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "dev": "vite",
    "lint": "eslint src --cache --cache-location .eslintcache",
    "lint:fix": "eslint src --cache --cache-location .eslintcache --fix",
    "preview": "vite preview",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@regicide/game-ui": "workspace:*",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.0.4",
    "vite": "8.1.5"
  }
}
