# Regicide Game

Regicide 的卡牌规则、单人对局和玩家决策所使用的领域语言。

## Language

**单人对局**：由一名玩家按照 Regicide 单人规则完成的游戏，使用八张手牌、两次 Solo Jester 机会和单人胜利评级。
_Avoid_: 单人房间、在线对局

**自动存档**：单人对局唯一的持久化恢复点，记录最近一次已接受玩家决策所产生的完整对局状态。
_Avoid_: 手动存档、事件日志、多个存档槽

**本地游戏会话**：浏览器中当前打开的单人对局及其恢复状态，由应用层独占并推进。
_Avoid_: React 状态、UI 状态、浏览器快照

**应用层提交**：被 core 接受的候选状态成功写入自动存档后，才替换本地游戏会话中的当前状态并向 UI 发布事件。
_Avoid_: core 接受即提交、先更新 UI 后存档

**不可恢复存档**：存在但无法通过存档 envelope 或 `parseGameState` 校验的自动存档；应用必须保留它并要求玩家显式清理后才能开始新局。
_Avoid_: 静默丢弃、自动覆盖、把损坏存档当作不存在

**无对局状态**：本地游戏会话没有可恢复的自动存档，也没有已创建的当前对局；只能由明确的开始新局操作离开。
_Avoid_: 启动时自动空局、隐式创建

**新局替换**：已有可恢复对局时，经玩家二次确认，用新对局的初始快照替换唯一自动存档；确认或保存失败都不得改变旧对局。
_Avoid_: 静默覆盖、先清除旧档

**玩家意图**：UI 送给应用层的、不包含执行身份的单人用户动作，包括出牌、让牌、承受伤害弃牌和使用 Solo Jester；应用层负责把它映射为 core 命令。
_Avoid_: UI 直接构造 `GameCommand`、UI 传入 actor 身份、选择下一位玩家

**会话快照**：应用层向 UI 提供的不可变读取结果，状态为 `uninitialized`、`no-game`、`active`、`unrecoverable-save`、`load-failed` 或 `stale`；只有 `active` 携带当前 `GameState`，它不是 UI 自己维护的副本。
_Avoid_: React state 作为事实源、平行 `GameView` 状态树

**只读查询门面**：接受会话快照中的深只读对局状态，委托 core 计算合法玩家意图、敌人状态和反击伤害的应用层查询；它不开放 `actorId` 或状态迁移。
_Avoid_: UI 去除只读类型、UI 调用 `dispatch`、复制规则计算

**提交事件**：一次成功写入自动存档并替换会话状态后，仅随该次应用操作结果交付一次的 `GameEvent[]` 表现反馈；它不参与恢复，也不是事件日志。
_Avoid_: 启动回放、拒绝事件、事件溯源

**应用结果**：会话操作向 UI 返回的可判别结果，区分 core 拒绝、存储失败和不可恢复存档；预期失败不是未分类异常。
_Avoid_: 所有失败都 throw、UI 猜测错误类型

**存档版本**：自动存档 envelope 版本和 core `schemaVersion` 必须是当前支持的版本；v1 不迁移未知版本，版本不匹配的内容属于不可恢复存档。
_Avoid_: 猜测字段、静默降级、无版本 JSON

**存档端口**：`game-application` 定义的单槽持久化契约，以结构化的加载、保存和清理结果隔离浏览器 key、JSON envelope 与 Web Storage API。
_Avoid_: 应用层直接调用 `localStorage`、端口暴露原始 key/value

**随机种子源**：创建新局时由应用层调用的可注入端口；H5 实现提供浏览器安全随机值，测试实现提供固定值，之后随机进度保存在 `GameState` 中。
_Avoid_: core 调用 `Math.random`、用 `Date.now` 隐式播种

**本地玩家**：应用层为单人对局固定的唯一玩家身份，同时是 core 的唯一玩家和起始玩家；它不是 UI 输入或远程身份。
_Avoid_: 玩家配置表单、UI 传入 `PlayerId`、本地账号

**已结束对局**：core 状态为胜利或失败、仍保留在唯一自动存档中的最终局面；刷新后恢复结算，直到玩家确认新局替换。
_Avoid_: 胜负后自动清档、自动开始下一局

**失效会话**：检测到其他浏览器标签页修改了唯一存档后，停止接受玩家意图、等待刷新恢复最新存档的本地游戏会话。
_Avoid_: 跨标签页合并、旧标签继续覆盖、实时同步

**会话初始化**：组合根创建无副作用的本地游戏会话后，显式执行的首次恢复入口；它加载存档、建立外部变化监听并确定初始会话状态，读取失败时由显式重试再次执行恢复。
_Avoid_: 构造函数读存档、初始化前执行玩家操作、静默重试

**组合根**：H5 页面中创建并连接唯一 `LocalGameSession`、浏览器存档适配器和随机种子源的入口，同时负责会话初始化与销毁。
_Avoid_: UI 组件自行 new 会话、全局隐式单例、组件直接取得端口

**清理不可恢复存档**：玩家确认后只删除不可恢复存档并使会话回到无对局状态的操作；它不会隐式创建新局。
_Avoid_: 清理并自动开局、用普通开始操作覆盖损坏档

**同步应用接口**：v1 的会话操作和存档、种子端口都在一次调用中返回结果；当前 core、localStorage 与浏览器安全随机源不引入异步边界。
_Avoid_: 无实际异步工作的 Promise、伪造 loading 阶段
