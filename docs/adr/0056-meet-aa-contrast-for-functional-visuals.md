# Meet AA contrast for functional visuals

v1 的 DOM 与 Canvas 功能性视觉统一满足 WCAG AA：普通文字与背景对比至少 `4.5:1`，大号文字至少 `3:1`，可操作边界、图标和规则状态标记至少 `3:1`。原创插画可作为装饰保留更自由的明暗关系，但位阶、花色、生命、攻击、护盾、选择、禁用和错误等决策信息必须使用独立达标的视觉 token，并继续避免只靠颜色编码。Chrome 文字缩放为 `200%` 时，DOM 外壳、弹窗、结算和规则参考必须重排且不裁切内容、不产生整页横向滚动；这一状态进入既定最小视口测试矩阵。
