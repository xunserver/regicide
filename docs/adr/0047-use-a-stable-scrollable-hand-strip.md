# Use a stable scrollable hand strip

v1 将最多八张手牌呈现为 Canvas 内单行、非重叠、可横向滚动的卡带，视觉顺序始终跟随 `game.players[0].hand`。点击卡牌切换选择并以上抬表现选中状态；底部操作栏固定在卡带滚动区域之外。UI 不提供扇形重叠、拖拽重排、自定义排序或由选择引起的牌序变化。这样窄竖屏仍能保留清晰卡面与独立命中区域，瞬时选择状态也不会演变成第二套手牌顺序。
