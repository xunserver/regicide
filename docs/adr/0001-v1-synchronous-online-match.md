---
status: superseded by ADR-0003
---

# Make v1 a synchronous online match

v1 采用同步在线对局：1 到 4 名玩家在同一场在线对局中完成回合，断线只提供短时重连，不支持离线数小时或数天后继续的异步回合。这样可以让首版围绕单一的回合推进、短时连接恢复和服务端权威状态建立边界，而不提前引入异步通知、长期超时、跨多局并发和复杂的对局唤醒机制。
